Dependencies:

```
npm install -g truffle
npm install -g ethlint
npm install -g web3
```

To run tests:

```
truffle test
```

To run linter:

```
solium -d .
```

Remember to lint and run tests before committing.
